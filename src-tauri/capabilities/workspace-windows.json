{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "workspace-windows",
  "description": "Minimum event and file-dialog capability for validated secondary SCS workspace windows",
  "windows": ["scs-workspace-*"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "dialog:default"
  ]
}
